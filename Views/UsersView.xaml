<UserControl x:Class="hci_restaurant.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:hci_restaurant.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1044">
    <Border Background="{DynamicResource Background}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0">
                <StackPanel Orientation="Horizontal">
                    <TextBox x:Name="SearchBox" FontSize="13" FontFamily="{DynamicResource MyFont}" Foreground="{DynamicResource BackgroundText}" Margin="50,0,0,0" 
                             Background="Transparent" BorderBrush="{DynamicResource BackgroundText}" BorderThickness="0,0,0,2" Height="28" VerticalContentAlignment="Center" Width="850"/>
                    <Button x:Name="ExitButton" Background="Transparent" Margin="55,20,20,20" Width="30">
                        
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
